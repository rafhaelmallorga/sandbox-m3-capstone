import { NavBar } from "./styled";

const Header = () => {
  return (
    <NavBar>
      <div>
        <h1>Meu Site</h1>
        <nav>
          <span>Logout</span>
        </nav>
      </div>
    </NavBar>
  );
};

export default Header;
